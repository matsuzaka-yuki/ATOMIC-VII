---
interface Props {
  class?: string;
  tag?: string;
}

const { class: className = "", tag = "div" } = Astro.props;
const Tag = tag as any;
---

<Tag class={`metal-panel-bg ${className}`}>
  <slot />
</Tag>
